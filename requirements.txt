# =============================================================================
# Statelix Python Dependencies
# =============================================================================
# このファイルでPythonの依存パッケージを管理します
# インストール: pip install -r requirements.txt
# =============================================================================

# --- Core Dependencies (必須) ---
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# --- Build Dependencies (ビルド時に必要) ---
pybind11>=2.10.0
setuptools>=61.0.0
wheel

# --- GUI Dependencies (GUIアプリ用) ---
PySide6>=6.4.0
matplotlib>=3.5.0

# --- Testing Dependencies (テスト用) ---
pytest>=7.0.0
pytest-cov>=4.0.0

# --- Optional: Data Processing ---
openpyxl>=3.0.0      # Excel読み込み
xlrd>=2.0.0          # 旧Excelフォーマット
statsmodels>=0.13.0  # 追加の統計モデル
